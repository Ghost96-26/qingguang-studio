# 本次公开包验证记录

日期：2026-09-05。范围是部署资料与脱敏导出工具，不是重新验收全部产品功能。

| 检查项 | 结果 |
|---|---|
| 允许名单源码导出 | 通过；排除环境、权重、素材、日志、数据库与第三方源码整包 |
| 本机已知密钥长字符串比对 | 通过；不输出密钥值 |
| 敏感模式、机器路径、文件类型与体积检查 | 通过 |
| Python 语法与 JSON 解析 | 通过 |
| PowerShell 下载/启动脚本语法解析 | 通过；未执行模型下载或服务重启 |
| 配置占位符展开及带空格目录 | 通过；临时目录验证 |
| 已有配置覆盖保护 | 通过；第二次运行拒绝写入，原文件保持一致 |
| 模型扩展名、大文件、二进制与凭据拒绝 | 通过；使用合成数据 |
| Git 暂存内容检查 | 通过；临时仓库验证暂存的敏感内容即使在工作区改掉仍会被拒绝 |

扫描器仅对一个已审阅的 HTTP 测试文件中的固定合成密码做精确例外处理，不整体跳过测试目录。Git 全局忽略文件在临时仓库测试中不可读，未影响上述索引内容检查结果。

输出包逐文件大小和 SHA256 见根目录 `PUBLIC-EXPORT-REPORT.json`。报告自身不在自己的哈希清单中。未对既有 Git 历史做扫描，未进行 GitHub 上传，也未开展新机完整环境安装、真实 GPU 推理和公网验收。
